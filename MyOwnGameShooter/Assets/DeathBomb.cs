using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DeathBomb : MonoBehaviour
{
    /*
    [SerializeField] GameObject dude;
    Player player;
    [SerializeField] float moveSpeed = 2f;
    float movementPerFrame;
    [SerializeField] bool isChase = true;

    void Start(){
    player = FindObjectOfType<Player>();
    
    }
    
    public void SpawnDude()
    {

         movementPerFrame = moveSpeed * Time.deltaTime;
        GameObject a =  Instantiate(  dude , transform.position, Quaternion.identity)as GameObject;

        a.GetComponent<Rigidbody2D>().velocity = new Vector2(0f, -10f);
           // a.transform.position = Vector2.MoveTowards(transform.position, player.transform.position, movementPerFrame);
        
      
        
                   currentPosition,targetPosition, movespeed*Time.deltaTime
        var 
         
         

            //Vector2.MoveTowards      public static Vector2 MoveTowards(Vector2 current, Vector2 target, float maxDistanceDelta);



    }*/
}
